# Bundle patch applied over the selected DSH profile when this package is listed
# in `dsh.profile.bundles`. Row ids are deployment-local; package names must
# match package.json.

- insert:
    - id: eco-permission-gate
      name: '@eco-dsh/eco-permission-gate'
    - id: eco-audit-chain
      name: '@eco-dsh/eco-audit-chain'
    - id: eco-memory-tree
      name: '@eco-dsh/eco-memory-tree'
