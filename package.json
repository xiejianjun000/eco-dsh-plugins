{
  "name": "eco-dsh-plugins",
  "version": "0.1.0",
  "private": true,
  "description": "dsh plugins ported from eco-agent: permission gate, audit chain, memory tree",
  "dsh": {
    "bundle": { "patch": "./cordis.patch.yml" }
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "node --test packages/*/test/"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.2.0"
  }
}
